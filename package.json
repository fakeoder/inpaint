{
  "name": "inpaint",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Static, frontend-only high-resolution object eraser for images (LaMa + onnxruntime-web WASM)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.2",
    "vite": "^6.4.3",
    "vitest": "^4.1.10"
  }
}
